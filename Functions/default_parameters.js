/* 
   জাভাস্ক্রিপ্টে ডিফল্ট প্যারামিটার (Default Parameters) সম্পর্কে শিখি।
   ডিফল্ট প্যারামিটার হলো এমন একটি ব্যবস্থা যেখানে ফাংশন কল করার সময় যদি কোনো আর্গুমেন্ট 
   না পাঠানো হয়, তবে ফাংশনটি নিজে থেকেই একটি পূর্বনির্ধারিত মান (Default Value) গ্রহণ করে।
*/

function add(num1 = 0, num2 = 0) {
  /* 
     ১. এখানে 'add' ফাংশনটি দুটি প্যারামিটার (num1, num2) গ্রহণ করে।
     ২. 'num1 = 0' এবং 'num2 = 0' লেখার মানে হলো, যদি ফাংশন কল করার সময় 
        এই মানগুলো না দেওয়া হয়, তবে জাভাস্ক্রিপ্ট এদের মান 'undefined' না ধরে '0' ধরবে।
     ৩. এটি কোডকে এরর (Error) বা NaN (Not a Number) হওয়া থেকে রক্ষা করে।
  */

  /* 
     ৪. console.log এর মাধ্যমে num1 এর বর্তমান মানটি প্রিন্ট করা হচ্ছে।
  */
  console.log("Number 1:", num1);

  /* 
     ৫. console.log এর মাধ্যমে num2 এর বর্তমান মানটি প্রিন্ট করা হচ্ছে।
  */
  console.log("Number 2:", num2);

  /* 
     ৬. num1 এবং num2 এর যোগফল বের করে কনসোলে দেখানো হচ্ছে।
  */
  console.log("Sum : ", num1 + num2);
}

/* 
   ৭. এখানে 'add()' ফাংশনটিকে কোনো আর্গুমেন্ট ছাড়াই কল করা হয়েছে।
   ৮. যেহেতু কোনো মান পাঠানো হয়নি, তাই ডিফল্ট প্যারামিটার অনুযায়ী num1 = 0 এবং num2 = 0 হবে।
   ৯. আউটপুট আসবে: 
      Number 1: 0
      Number 2: 0
      Sum : 0
*/
add();

/* 
   সারসংক্ষেপ (Learning Note):
   - ডিফল্ট প্যারামিটার ব্যবহার করলে ফাংশন আরও বেশি শক্তিশালী (Robust) হয়।
   - যদি আমরা add(5) কল করতাম, তবে num1 হতো 5 এবং num2 তার ডিফল্ট মান 0 নিত।
   - যদি আর্গুমেন্ট পাঠানো হয়, তবে ডিফল্ট মানটি ওভাররাইট (Overwrite) হয়ে যায় এবং পাঠানো মানটিই কাজ করে।
   - এটি মূলত 'undefined' ভ্যালু হ্যান্ডেল করার একটি আধুনিক ও সহজ উপায়।
*/
